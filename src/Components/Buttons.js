import styled from "styled-components";

const PrimaryButton = styled.button`
  background: red;
  padding: 14px 24px;
  color: white;
  border: none;
  font-size: 1rem;
`;

export default PrimaryButton;
